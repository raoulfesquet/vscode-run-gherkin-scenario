name: Publish Extension

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Package VSIX
        run: npx @vscode/vsce package --allow-missing-repository

      - name: Publish to VS Code Marketplace
        run: npx @vscode/vsce publish --pat ${{ secrets.VSCE_PAT }} --allow-missing-repository

      - name: Create Open VSX namespace
        continue-on-error: true
        run: npx ovsx create-namespace raoulfesquet --pat ${{ secrets.OVSX_TOKEN }}

      - name: Publish to Open VSX
        run: npx ovsx publish *.vsix --pat ${{ secrets.OVSX_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: "*.vsix"
          generate_release_notes: true
